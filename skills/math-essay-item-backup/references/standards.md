성취기준·성취수준을 문항에 붙일 때 지키는 규칙 — 출처 확인, 적용 교육과정 판정(2015/2022)과 수준 체계 처리, 성취기준→평가 요소→채점 요소 정렬, 선언과 실행의 일치, 성취수준 점수 구간 재분할, 교과 역량 표기.

## 1. 출처 원칙 — 지어내지 않는다

### 1.1 근거 자료

성취기준 문구와 성취기준별 성취수준은 **NCIC 국가교육과정정보센터에 공시된 자료 가운데 §1.6으로 판정한 적용 교육과정의 것**만 근거로 삼는다. 교과서·문제집·블로그·기억은 근거가 아니다. 성취기준별로 공시된 것의 이름과 칸 수가 개정판에 따라 다르다 — **2022 개정은 성취수준 A~E 5단계, 2015 개정은 평가기준 상·중·하 3단계**다(§1.6의 표).

| 자료 | 게시 URL |
|---|---|
| (중등)2022 개정 교육과정에 따른 성취수준(수학) — 평가원 발행, 87면 | `https://ncic.re.kr/bbs/standard/view/772.do` |
| (평가원)2022 개정 교육과정에 따른 초·중학교 수학과 성취수준 개발 연구 — 275면 | 같은 게시글 |
| 수학과_2015평가기준개발연구 — 중학교 2015 적용 시의 근거이자 2022 대조용 | `https://ncic.re.kr/bbs/standard/view/735.do` |
| 같은 계열 고등학교 2015 자료 — 고등학교 2015 적용 시의 근거 | `https://ncic.re.kr/bbs/standard/view/753.do` |

첨부 URL 형식은 `https://ncic.re.kr/bbs/download.do?articleIdx={게시글번호}&fileName={타임스탬프}.pdf`이고, 게시판 목록은 `https://ncic.re.kr/bbs/standard/list.do`(중2022 → 772, 고2022 → 780, 중2015 → 735, 고2015 → 753)다.

### 1.2 확인 절차

R1-1. 0단계에서 **성취기준 코드와 문구를 통째로 확보한다** — 사용자에게 입력받거나, 아래 원문 확보 도구(`tools/ncic_fetch.py`)로 **NCIC 공시 자료에서 직접 가져온다.** 코드만 받고 문구를 에이전트가 채우지 않는다.
R1-2. 성취기준별 성취수준(2022 개정 A~E · 2015 개정 평가기준 상·중·하 — §1.6)도 사용자에게 받거나, **적용 교육과정의** 자료에서 직접 읽어 온다. 성취기준 문구에서 유추해 쓰지 않고, 다른 개정판의 자료에서 끌어오지 않는다.
R1-3. 성취기준이 여러 개면 **채점 대상 하나**를 사용자에게 확정받는다(§3).
R1-4. 최종 문서에 들어간 성취기준 문구·성취수준 진술은 원문과 **글자 단위로 같아야 한다**. 줄이거나 다듬지 않는다.

#### 원문 확보 도구 — `<스킬 루트>/tools/ncic_fetch.py`

사용자가 문구를 붙여넣지 않으면 이 도구로 §1.1의 자료를 직접 가져온다. **지어내기 금지 원칙은 그대로다** — 이 도구는 원문을 손에 넣는 경로일 뿐, 없는 진술을 만들어 주지 않는다. 도구가 못 찾은 칸은 R1-5대로 비운다.

필요한 것은 `python3`와 `olefile`(`pip3 install olefile`), 그리고 NCIC 접속이다. 산출물(.hwp/.pdf/.txt)은 전부 **작업 폴더**에 생긴다. 스킬 폴더는 건드리지 않는다.

**⑴ 무엇이 붙어 있는지 본다**

```bash
S=<스킬 루트>; WORK=<작업폴더>; cd "$WORK"
python3 "$S/tools/ncic_fetch.py" list 735
```

```
게시글 735 — (중) 2015 개정 교육과정에 따른 평가기준 및 연구 보고서_ 수학과
  https://ncic.re.kr/bbs/standard/view/735.do
  [1] (중학교)2015 개정 교육과정에 따른 평가기준(수학).hwp  (hwp, 507KB)
      https://ncic.re.kr/bbs/download.do?articleIdx=735&fileName=1487124064819.hwp
  [2] 수학과_2015평가기준개발연구.pdf  (pdf, 5,767.7KB)
      https://ncic.re.kr/bbs/download.do?articleIdx=735&fileName=1487124065172.pdf
```

§1.1의 `fileName={타임스탬프}`는 **게시글 페이지 HTML의 다운로드 링크에 그대로 들어 있다.** 도구가 그것을 긁어 쓴다. 사람이 추측하거나 지어낼 값이 아니다.

**⑵ 받아서 본문을 뽑고 성취기준을 찾는다 — 한 번에**

```bash
python3 "$S/tools/ncic_fetch.py" fetch 중2015 --find "[9수04-18]"
```

별칭 `중2022`·`고2022`·`중2015`·`고2015`는 §1.1의 게시글 번호(772·780·735·753)로 풀린다. 숫자를 그대로 써도 된다. 도구는 이 별칭 표를 **이 문서 §1.1에서 읽는다** — 번호를 고치려면 §1.1을 고친다.

기대 출력(중2015·[9수04-18] 실행 로그 그대로):

```
[받는 중] https://ncic.re.kr/bbs/download.do?articleIdx=735&fileName=1487124064819.hwp
[저장] <작업폴더>/ncic735_(중학교)2015_개정_교육과정에_따른_평가기준(수학).hwp  (519168바이트)
[추출] <작업폴더>/ncic735_(중학교)2015_개정_교육과정에_따른_평가기준(수학).txt  (문단 848개, 24251자, 표 34개 / 칸 611개)

── 성취기준 [9수04-18] ──────────────────────────────
자료      : ncic735_(중학교)2015_개정_교육과정에_따른_평가기준(수학).hwp (게시글 735)
읽은 방법 : 표 구조(HWPTAG_TABLE/LIST_HEADER)
수준체계  : 2015-3
            2015 개정 평가기준 — 상·중·하 3단계. 서식의 성취수준란이 A~E 5칸이므로 그대로 넣을 수 없다. …
성취기준  : 삼각비를 활용하여 여러 가지 문제를 해결할 수 있다.
수준 진술 :
  상     삼각비를 활용하여 다양한 실생활 문제를 해결할 수 있다.
  중     삼각비를 이용하여 삼각형의 한 변의 길이와 넓이를 구할 수 있다.
  하     삼각비를 이용하여 직각삼각형에서 한 변의 길이를 구할 수 있다.
경고      : 없음
```

**⑶ `수준체계` 줄을 반드시 읽고 §1.6의 분기 입력으로 쓴다.**

| 값 | 뜻 | 다음 |
|---|---|---|
| `2015-3` | 상·중·하 3단계 | 서식의 A~E 5칸과 맞지 않는다. §1.6으로 적용 교육과정이 맞는지 확인하고 **§1.7대로** 배치한다 |
| `2022-5` | A~E 5단계 | 서식 5칸과 그대로 맞는다 |
| `미판정` | 수준 라벨을 못 찾았다 | 목차·부록을 짚었거나 표 구조가 다르다. `.txt`를 열어 사람이 확인한다 |

**⑷ 병합된 수준 칸은 병합 그대로 나온다(R1-10).** 도구는 표의 세로 병합(rowSpan)을 읽어 어느 수준들이 한 칸인지 그대로 알린다. 다섯 줄을 채우려고 나누지 마라.

```
  B·C   삼각형의 닮음 조건을 이해하고, 이를 이용하여 주어진 도형에서 닮음인 두 삼각형을 찾을 수 있다.
        ※ 원문에서 B·C가 한 칸으로 병합되어 있다. 병합 그대로 옮긴다(R1-10).
```

**⑸ 2015 자료의 `[평가준거 성취기준 ①/②]`는 묶음으로 갈라져 나온다.** 한 성취기준 아래 상·중·하가 두 벌 붙는 경우다. 어느 평가준거를 채점 대상으로 삼을지 R1-3대로 사용자에게 확정받는다.

**⑹ 기계가 읽어야 하면 `--json`.**

```bash
python3 "$S/tools/ncic_fetch.py" find 772 --find "[9수03-13]" --json > std.json
```

`level_scheme` / `matches[].curriculum_standard` / `matches[].groups[].levels[].label·statement·merged` / `matches[].warnings`가 들어 있다. 슬롯 JSON의 `std*_text`·`std*_A`~`std*_E`에는 `statement`를 **손대지 말고 그대로** 옮긴다(R1-4·R1-11).

**⑺ 이미 받아 둔 파일에서만 찾기 / 망이 막혔을 때**

```bash
python3 "$S/tools/ncic_fetch.py" extract ./ncic735_….hwp --find "[9수04-18]"   # 표 구조로 정확히
python3 "$S/tools/ncic_fetch.py" find    ./ncic735_….txt --find "[9수04-18]"   # 되돌림 경로
```

`.txt`에는 표 구조가 없어 병합 칸이 어긋날 수 있다. 그때 도구는 「표 구조 없이 줄 단위로 읽었다」를 경고로 찍는다. 경고가 뜬 값은 확보한 것으로 치지 말고 원문과 대조한다.

**종료 코드 — 실패를 조용히 넘기지 않는다**

| 코드 | 뜻 | 할 일 |
|---|---|---|
| 0 | 성공 | — |
| 2 | 사용법·인자 오류 | 명령을 고친다 |
| 3 | `olefile` 없음 | `pip3 install olefile`. 설치가 막히면 `--ext pdf`로 PDF를 받아 사람이 읽고 R1-1로 입력받는다 |
| 4 | 네트워크 실패 | 사람이 브라우저로 받은 .hwp를 주고 `extract`로 잇는다. 사설 인증서 환경은 `--insecure` |
| 5 | 게시글 없음 | §1.1의 번호를 확인한다 |
| 6 | 첨부 없음 | `--ext all`로 다시 본다 |
| 7 | HWP 파싱 실패 | 암호·배포용 문서이거나 내려받기가 잘렸다. 다시 받거나 PDF로 간다 |
| 8 | 성취기준 코드 못 찾음 | 코드 표기와 **학교급·개정 연도가 맞는 게시글**인지 본다(§1.6) |

**이 도구가 하지 않는 것.** 성취기준별 성취수준 표만 읽는다. §1.4의 **영역별 성취수준**은 성취기준 코드로 걸리지 않으므로 추출한 `.txt`에서 사람이 찾고, 두 표가 어긋나면 R1-9를 따른다. 도구가 `경고`를 하나라도 찍은 칸은 **확보하지 못한 것으로 보고** R1-5·R1-6대로 비우고 `missing`에 남긴다.

### 1.3 확인하지 못했을 때

R1-5. 근거를 확보하지 못한 성취수준은 **칸을 비운다.** 그럴듯한 문장을 넣지 않는다.
R1-6. 작업 노트(스크래치패드 JSON)의 최상위 `missing` 키에 문자열 배열로 남긴다. 한 결손이 한 줄이다.

```json
"missing": ["MISSING: std2_A~std2_E — [9수03-13] 성취수준 근거 미확보(NCIC 772 미확인). 사용자 확인 필요."]
```

`missing`은 작업 노트에만 두고 슬롯 JSON(`content*.json`)에는 넣지 않는다. 빌더가 모르는 키라 조용히 버려지고, 그러면 R1-8 검사가 통과해 버린다.

R1-7. 초안 단계에서 임시로 넣어야 하면 같은 칸 안에 꼬리표를 붙인다.

```
[9수03-13] 삼각형의 닮음 조건을 이해하고, 이를 이용하여 두 삼각형이 닮음인지 판별할 수 있다. ※ 미확인 — NCIC 공시 자료로 확인 후 확정
```

R1-8. **꼬리표가 남은 채로 최종 HWPX를 빌드하지 않는다.** 검사: 슬롯 JSON과 빌드 산출물 양쪽에서 `미확인` 0건, `MISSING` 0건. 빌드 산출물의 텍스트는 §3 R3-3의 추출 명령으로 뽑는다.

### 1.4 두 표가 어긋날 때

같은 자료 안에서도 **성취기준별 성취수준**(인쇄면 20면 계열)과 **영역별 성취수준**(39면 계열)의 진술이 다를 수 있다. [9수01-06]의 B수준이 실제 사례다.

- 나쁜 예 — 두 표를 섞어 적었다.
  > **B** 유한소수 또는 순환소수로 나타낼 수 있는 분수의 특징을 이해하고, 순환소수를 분수로 나타낼 수 있으며 이를 통해 유리수와 순환소수의 관계를 **설명할 수 있다.**

- 좋은 예 — 성취기준별 성취수준 표에서만 옮기고, 어느 표인지 각주로 밝혔다.
  > **B** 유한소수 또는 순환소수로 나타낼 수 있는 분수의 특징을 이해하고, 순환소수를 분수로 나타낼 수 있으며 이를 통해 유리수와 순환소수의 관계를 **이해한다.**
  >
  > ※ 성취기준별 성취수준 표(S1 인쇄면 20면)에서 옮김. 같은 자료의 영역별 성취수준 표(39면)는 같은 수준을 "설명할 수 있다"로 적고 있다.

R1-9. 평가 개요와 문항 정보의 성취수준란에는 **성취기준별** 진술을 쓴다 — 2022 개정이면 성취기준별 성취수준, 2015 개정이면 성취기준별 평가기준(§1.6)이다. 영역별 성취수준을 쓰려면 각주에 그렇게 밝힌다.

### 1.5 원문 그대로 옮긴다

R1-10. 원문에서 여러 수준이 같은 진술로 병합되어 있으면 **병합 그대로** 옮긴다. 다섯 줄을 채우려고 없는 문장을 만들지 않는다. 경기 자료집 고1 문항의 실물이 그렇다(`references/gyeonggi-2025-extract.txt` 125~133행. 원문은 A·B가 한 칸, C·D가 한 칸, E가 한 칸이다).

```
[10공수1-01-03]   A
다항식의 인수분해를    B   다양한 방법으로 다항식의 인수분해를 할 수 있다.
할 수 있다.        C
                 D   인수분해 공식을 이용하여 다항식의 인수분해를 할 수 있다.
                 E   안내된 절차에 따라 간단한 다항식의 인수분해를 할 수 있다.
```

**서식에서 병합을 표현하는 방법.** `std1_A`~`std1_E`는 각각 별개의 셀이고 빌더는 셀을 병합하지 못한다(`references/hwpx-build.md` §2 T2). 그래서 원문이 병합해 둔 자리는 **같은 문장을 해당 칸마다 반복해 넣고**, 원문이 병합 표기였음을 각주로 밝힌다. 이 복제가 허용되는 것은 **원문이 스스로 두 수준을 같은 진술로 묶었을 때뿐**이다. 원문에 A~E라는 축 자체가 없는 경우(2015 개정 자료)는 병합이 아니라 다른 문제이며 §1.7이 따로 다룬다.

R1-11. 원문의 오식도 고치지 않고 그대로 옮긴다. 실제 사례는 NCIC 자료 S1의 **영역별 성취수준**(인쇄면 39~41면) 가치·태도 진술에 있는 `인식하고.`(쉼표 자리에 마침표), `사칙계산 의`(불필요한 띄어쓰기)다. 같은 자리에서 내용 요소는 "수 체계의 **논리적** 아름다움"인데 영역별 성취수준은 "수 체계의 **절차적** 아름다움"으로 적혀 있고, 이것도 그대로 옮긴다. — 이 두 오식은 경기 자료집이 아니라 NCIC S1에 있다. `gyeonggi-2025-extract.txt`에는 0건이므로 거기서 찾지 마라.

### 1.6 적용 교육과정 판정 — 실시 연도 × 학년 → 자료와 수준 체계

2022 개정 교육과정은 중1·고1 2025년, 중2·고2 2026년, 중3·고3 2027년 순차 적용된다. **평가 실시 연도와 학년 두 값으로 판정한다.** 학년만 보고 정하지 않는다 — 같은 중3이라도 2026년 실시는 2015 개정, 2027년 실시는 2022 개정이다.

| 실시 연도 | 중1 · 고1 | 중2 · 고2 | 중3 · 고3 |
|---|---|---|---|
| 2024년 이전 | 2015 개정 | 2015 개정 | 2015 개정 |
| 2025년 | **2022 개정** | 2015 개정 | 2015 개정 |
| 2026년 | 2022 개정 | **2022 개정** | 2015 개정 |
| 2027년 이후 | 2022 개정 | 2022 개정 | **2022 개정** |

판정 결과로 **근거 자료(NCIC 게시글)와 성취수준 체계가 함께** 갈린다.

| 적용 교육과정 | 학교급 | NCIC 게시글 | 성취기준별로 공시된 것 | 채울 칸 |
|---|---|---|---|---|
| 2022 개정 | 중학교 | 772 | 성취**수준** A·B·C·D·E (5단계) | `std*_A`~`std*_E` 다섯 |
| 2022 개정 | 고등학교 | 780 | 성취**수준** A·B·C·D·E (5단계) | `std*_A`~`std*_E` 다섯 |
| 2015 개정 | 중학교 | 735 | 평가**기준** 상·중·하 (3단계) | `std*_A`·`std*_C`·`std*_E` 셋 (§1.7) |
| 2015 개정 | 고등학교 | 753 | 같은 계열이나 **이 스킬이 원문을 확인한 적 없음** | 원문 확인 후 결정 |

735는 원문 텍스트를 전수로 확인했다 — 표에서 **수준 라벨 칸으로 쓰인 것이 상 68 · 중 68 · 하 68**이고 `A수준`·`B수준`은 0회다. (라벨 칸만 세어야 한다. 문서 전체의 단순 문자열 빈도는 상 128 · 중 112 · 하 471로, `이상`·`중학교`·`이하` 같은 낱말 속의 글자가 섞인다.) 문서가 성취기준별로 싣는 것의 이름 자체가 "성취수준"이 아니라 **평가기준**이다. 753은 열어 본 적이 없으므로 칸 수를 추정하지 않는다. **고등학교 2015로 판정되면 753 원문에서 체계를 먼저 확인하고, 확인하지 못하면 R1-5대로 비운다.**

2015 자료 안에서 A~E 표기를 발견했더라도 그것이 **성취기준별** 표인지 먼저 확인한다. 영역별·단원별 표라면 R1-9에 따라 성취수준란에 옮기지 않는다(§1.4와 같은 함정이다).

R1-12. 0단계에서 **실시 연도와 학년을 먼저 받아 위 두 표로 판정하고**, 적용 교육과정·게시글 번호·수준 체계를 작업 노트에 남긴다. 성취기준 문구를 먼저 받고 판정을 뒤로 미루지 않는다 — 어느 자료에서 문구를 받아야 하는지가 이 판정으로 정해지기 때문이다.

```json
"curriculum": {"year": 2026, "school": "중학교", "grade": 3, "revision": "2015", "ncic": 735, "levels": "상·중·하", "slots": 3}
```

같은 내용을 `purpose`에 각주 한 줄로 남긴다(R1-14의 첫째 줄). 2022 개정으로 판정되면 §1.7은 적용되지 않고 다섯 칸을 전부 채운다.

### 1.7 3단계(상·중·하) 자료를 5칸 서식에 옮긴다

서식의 성취수준란은 `std1_A`~`std1_E` **다섯 칸**이다(`references/hwpx-build.md` §2 T2, 좌표 (2,4,3)~(2,4,7)). 다섯 행은 각각 별개의 셀이고 옆 칸에 라벨 `A`·`B`·`C`·`D`·`E`가 인쇄되어 있다. 2015 개정으로 판정되면 근거 자료에는 진술이 **셋**뿐이다.

R1-13. **상 → `std1_A`, 중 → `std1_C`, 하 → `std1_E`. `std1_B`와 `std1_D`는 빈 문자열로 둔다.** 같은 문장을 두 칸에 복제해 다섯 칸을 채우지 않는다. `std2_*`도 같다.

근거 세 가지다.

⑴ **R1-5** — 2015 자료는 "B수준의 진술"에 대해 아무 말도 하지 않는다. 상의 문장을 B칸에 복제하면 문서가 "이 문장이 B수준의 진술이다"라는, 원문에 없는 주장을 하게 된다. 문자열을 새로 쓰지 않았어도 **없는 근거를 만든 것**이다. 근거가 없는 칸은 비운다.

⑵ **R1-10과의 구분** — R1-10의 "병합 그대로"는 **원문이 병합해 둔 경우**의 규칙이다. 경기 자료의 `[10공수1-01-03]`은 원문이 A·B를 한 칸에 적었으므로, 두 칸에 같은 문장을 넣는 것이 곧 원문을 옮기는 일이다. 2015 자료에는 `std*_A~E`가 세우는 5단계 축 자체가 없다. 없는 축에 병합을 만들어 붙이는 것은 R1-10의 적용이 아니라 확장이고, R1-10이 금지한 "**다섯 줄을 채우려고 없는 문장을 만드는 것**"과 같은 자리에 선다. 서식에 칸이 있다는 것은 담을 수 있다는 뜻이지 채워야 한다는 뜻이 아니다(반대 방향의 같은 원칙이 R5-6이다).

⑶ **가역성** — 상·중·하 셋을 A·C·E에 하나씩 놓으면 진술의 **개수·차례·문면**이 원문과 같아 표에서 원문을 되짚을 수 있다. 상을 A·B에, 하를 D·E에 복제하면 나중에 그 표를 읽는 사람이 원문이 병합 표기였는지(R1-10) 3단계를 늘린 것인지 구분할 수 없다. 되짚을 수 없는 표기는 쓰지 않는다.

A·C·E를 고르는 까닭은 위계를 어긋나지 않게 하려는 것뿐이다 — 최상위 진술은 최상위 칸에, 최하위 진술은 최하위 칸에, 가운데는 가운데에 둔다. 검토한 다른 후보는 다음 둘이고, 둘 다 쓰지 않는다.

| 후보 | 기각 이유 |
|---|---|
| 상 → A·B 복제 / 중 → C / 하 → D·E 복제 | 각 수준의 **폭**(상이 A와 B 둘을 덮는다)을 주장하는데, 2015 3단계와 2022 5단계 사이에 공시된 대응표가 없으므로 그 폭에 근거가 없다. 위 ⑴~⑶ 전부에 걸린다 |
| 상 → A / 중 → B / 하 → C, D·E 공란 | 최하위 진술을 가운데 칸에 놓아 위계가 어긋난다. 지면의 E칸이 비어 "가장 낮은 수준의 기술이 없는 문서"로 읽힌다 |

**빈 B·D는 결함이 아니다.** §2.3의 판단("칸이 비어 있는 것은 결함이 아니다")이 그대로 적용된다. 오히려 빈 칸이 지면에서 3단계 자료임을 드러내므로 다섯 칸이 다 찬 표보다 낫다 — 다 채우면 각주 한 줄이 떨어져 나가는 순간 문서가 2015 자료를 2022의 5단계인 것처럼 말하게 되고, 검증하는 쪽도 772에서 대조하려 든다.

R1-14. 3단계를 옮겼으면 `purpose`에 각주 **두 줄**을 넣는다. 그대로 붙여넣고 중괄호만 바꾼다.

```
※ 적용 교육과정 — {2026}년에 실시하는 {중학교} {3}학년 평가이므로 {2015} 개정 교육과정을 적용한다(2022 개정 {중학교} {3}학년 적용은 {2027}년부터).
※ 성취수준은 {2015} 개정 교육과정 평가기준(NCIC 게시글 {735})의 상·중·하 3단계를 옮긴 것이며, 그 자료에는 A~E 5단계가 공시되어 있지 않아 B·D 칸을 비웠습니다.
```

`purpose`는 문자열 배열이므로 각 각주를 한 원소로 넣고 앞에 빈 문자열 한 줄을 둔다. 지면 문체가 합쇼체면 둘째 줄처럼 `~비웠습니다`로, 평서체면 `~비웠다`로 맞춘다. 2022 개정으로 판정된 문항은 둘째 줄을 쓰지 않고 첫째 줄만 남긴다.

R1-15. 빈 `std1_B`·`std1_D`는 **`missing`에 넣지 않는다.** `missing`(R1-6)은 "근거를 확보하지 못했으니 확인해서 채워라"는 표시다. B·D는 확보에 실패한 것이 아니라 **원문에 없는 층**이므로, 채우라는 신호를 남기면 다음 사람이 지어내게 된다. R1-12의 `curriculum` 키에 함께 적는다.

```json
"curriculum": {"year": 2026, "school": "중학교", "grade": 3, "revision": "2015", "ncic": 735,
               "levels": "상·중·하",
               "slot_map": {"std1_A": "상", "std1_C": "중", "std1_E": "하"},
               "blank": ["std1_B", "std1_D"],
               "note": "2015 자료에 A~E 5단계 없음. 비운 것이 정상이며 채우지 말 것."}
```

검사 — 3단계로 판정했고 `std1_text`가 차 있으면 `std1_A`·`std1_C`·`std1_E`가 모두 차 있고 `std1_B`·`std1_D`가 모두 빈 문자열이며, 다섯 값 가운데 같은 문자열이 둘 이상 없다. 5단계로 판정했으면 다섯 칸이 모두 찬다(원문이 병합한 자리는 같은 문장이 반복되고, 그 사실이 각주에 있다).

**두 종류의 빈 칸을 헷갈리지 않는다.** 3단계 변환의 `std1_B`·`std1_D`는 **원문에 없어서** 빈 것이라 채우면 안 되고 `missing`에도 넣지 않는다. 5단계 자료인데 일부 수준을 확보하지 못해 빈 것은 **채워야 할 결손**이라 R1-5로 비워 두되 R1-6의 `missing`에 반드시 남긴다. 지면에서는 둘이 똑같이 빈 칸으로 보이므로, 어느 쪽인지는 작업 노트의 `curriculum`·`missing`으로만 갈린다.

**이 분기는 성취수준 점수 구간(`level_*`)과 무관하다.** 두 층의 차이는 §4.0에 있다.

## 2. 정렬 규칙 — 성취기준 → 평가 요소 → 채점 요소

### 2.1 사슬

```
성취기준 (교육과정 문서에 주어짐)
   ↓ 문항에 맞게 구체화
평가 요소 (문항 개발 前, 성취기준·성취수준 분석에서 도출. 명사형 어구)
   ↓ 문항의 〈조건〉·맥락을 반영해 관찰 가능한 수행으로 쪼갬
채점 요소 (문항 개발 後, 짧은 명사구 6~16자)
   ↓
급간별 수행 수준
```

R2-1. **모든 채점 요소는 정확히 하나의 평가 요소에 속한다.** 어느 평가 요소에도 속하지 않는 채점 요소가 있으면 결함이다. 검사: 고아 채점 요소 0건.
R2-2. **모든 평가 요소는 성취기준란에 적힌 코드 중 하나에 속한다.** 검사: 고아 평가 요소 0건.
R2-3. 채점기준표의 문항 열에 **문항 번호 / 성취기준 코드 / 그 문항의 평가 요소**를 세 줄로 넣고 세로 병합한다. 채점기준을 읽는 자리에서 성취기준이 보이게 한다. 평가 요소는 10~17자로 줄인다.

```
문항 3
[9수03-12]
주문 방안 판단하고 설득하기
```

### 2.2 근거 — 경기 자료 일러두기

> **지수법칙 등 성취기준에 따른 성취수준과 관련 없는 요소들이 채점기준에 포함되지 않도록 주의해야 함.**
> — 2025 경기 논술형 평가 Ⅱ, 고1 문항 〈채점 시 유의점〉 (원문 추출본 278행)

이 문장은 두 방향으로 읽는다. ⑴ 성취기준 밖 내용을 채점하지 말 것. ⑵ 채점하지 않을 내용이면 성취기준란에도 올리지 말 것.

### 2.3 사례 — [9수03-13]을 걷어낸 판단

피자 문항의 v2에는 둘째 성취기준이 들어 있었다.

- 나쁜 예 (v2)
  - 성취기준란: `[9수03-13] 삼각형의 닮음 조건을 이해하고, 이를 이용하여 두 삼각형이 닮음인지 판별할 수 있다.`
  - 4차시 활동: `세 변 / 두 변과 끼인각 / 두 각을 각각 주고 삼각형을 그려 보며 SSS·SAS·AA 닮음 조건 발견하기`
  - 5차시 활동: `평행선 사이의 선분의 길이의 비를 삼각형의 닮음으로 설명하고 문제 해결하기`
  - 그런데 채점 요소 6개는 **전부 [9수03-12]**였다. 삼각형의 닮음 조건을 채점하는 요소는 하나도 없었다.

- 좋은 예 (v4)
  - `std2_text`와 `std2_A`~`std2_E`를 빈 문자열로 비웠다.
  - 4차시를 [9수03-12]의 실생활 전이로 교체: `지도 축척 — 축척이 주어진 지도에서 길이의 비와 넓이의 비를 구분하여 실제 거리와 실제 넓이를 구하고, 두 비가 다른 까닭을 말하기`
  - 5차시를 단원 개념도 정리·일반화 다듬기·자기평가로 교체.
  - `purpose`와 〈채점 시 유의점〉의 [9수03-13]·[9수03-14] 언급을 삭제했다.

판단 근거는 위 일러두기다. 이 평가의 평가 요소는 [9수03-12]뿐이고 삼각형의 닮음 조건은 채점 대상이 아니므로, 성취기준란에 올려 두면 "가르쳤다고 선언했지만 평가하지 않는" 상태가 된다.

**주의 — 반대 방향의 오판이 먼저 있었다.** 수준기술식 전환 단계의 다중렌즈 감사는 "둘째 성취기준 블록 공란"을 결함으로 지적했고, 그 지적을 받아 [9수03-13]과 성취기준별 성취수준 `std2_A`~`std2_E`를 채워 넣었다. v4에서 이를 다시 걷어냈다. **칸이 비어 있는 것은 결함이 아니다.** 결함은 선언하고 가르치지 않는 것, 또는 선언하고 평가하지 않는 것이다.

### 2.4 성취기준 밖 요소 금지 목록

R2-4. 다음은 채점 요소가 될 수 없다. 〈채점 시 유의점〉에 명시한다.

> 성취기준 밖 요소를 채점기준에 넣지 않습니다. 본 문항의 성취기준은 [9수03-12] 하나입니다. 삼각형의 닮음 조건[9수03-13], 평행선 사이 선분의 길이의 비[9수03-14], 닮은 입체도형의 부피의 비는 이 문항의 채점 요소가 아니며, 공지문의 문장력·글씨·창의적 아이디어(음료 구매 제안 등)도 채점 요소가 아닙니다.

R2-5. 단위 표기·맞춤법·띄어쓰기·필체는 감점 대상이 아니다. 다만 〈조건〉이 명시적으로 요구한 표현 형식(용어 병기, 문단 수 등)은 채점 요소에 반영한다.
R2-6. **가치·태도는 독립된 채점 요소로 점수화하지 않는다.** 관찰 대상으로만 둔다. 종전 〈채점 시 유의점〉 C에 있던 "교과협의회가 태도에 별도 배점이나 가점을 줄 수 있다"는 재량 조항은 삭제되었고, 다음으로 대체되었다.

> 평가 요소의 가치·태도는 독립된 채점 요소로 점수화하지 않고 1-(3)②의 '설득하는 공지문으로 조직하기' 안에서 관찰합니다. 태도에 별도 배점이나 가점을 두지 않습니다.

### 2.5 역방향 대조표

R2-7. 채점 요건을 다 쓴 뒤 **역방향 대조표**를 만든다. 각 채점 요건이 발문·조건의 어느 문장에서 나왔는지 한 줄씩 적는다.

```
1-(1)(가) 지름의 비 4:5:6   → (1) "지름의 비를 가장 간단한 자연수의 비로 나타내고"
1-(2)(다) 세 용어           → 조건④
1-(3)①(다) 소수 첫째 자리    → 조건⑥
```

검사 두 가지. ⑴ 근거 없는 채점 요건 0건. ⑵ 모든 조건이 최소 한 채점 요소에 걸려 있을 것. 피자 문항은 22항목 대조에서 양방향 0건이었다.

## 3. 선언한 성취기준은 반드시 가르치고 평가한다

R3-1. 성취기준란에 적은 각 코드는 **세 곳에 모두** 나타나야 한다.

| # | 나타나야 하는 곳 | 검사 |
|---|---|---|
| ⑴ | 차시 흐름의 최소 1차시 활동 | 그 코드의 성취기준 문구에서 **핵심 명사 2개**를 뽑아(예: [9수03-12] → `닮음`, `닮음비`) 어느 `lesson{n}_act`가 둘 다 포함하는지 본다. 포함하는 차시가 0개면 실패 |
| ⑵ | 최소 1개 문항의 문항 정보 성취기준란 | `item{n}_element` 끝의 `([코드])` 표기가 있는가 |
| ⑶ | 최소 1개 채점 요소 | `rubric_rows`의 `item` 열에 그 코드가 있는가 |

셋 중 하나라도 없으면 **그 코드를 성취기준란에서 지운다.** 차시만 있고 채점 요소가 없으면 결함, 채점 요소만 있고 차시가 없어도 결함이다.

⑴의 핵심 명사 2개는 코드마다 한 번 정해 작업 노트에 적어 두고(`"std_keywords": {"9수03-12": ["닮음", "닮음비"]}`) 검사할 때마다 같은 것을 쓴다. 매번 다시 고르면 통과·불통과가 흔들린다. 이 문서가 쓰는 슬롯 키(`std1_*`·`std2_*`·`item{n}_element`·`lesson{n}_act`·`lesson{n}_eval`·`rubric_rows`·`level_*_score`·`level_*_desc`·`purpose`·`partial`)의 좌표와 전체 목록은 `references/hwpx-build.md` §2에 있다.

R3-2. **쓰지 않을 성취기준은 칸을 통째로 비운다.** 반쯤 채우지 않는다. 문구만 넣고 성취수준을 통째로 비우는 것, 성취수준만 넣고 문구를 비우는 것 모두 금지. 검사: `std2_text`가 빈 문자열이면 `std2_A`~`std2_E`도 전부 빈 문자열.

여기서 금지하는 "반쯤"은 **문구와 성취수준 블록 사이의 반쪽**이다. 3단계 자료를 옮겨 `std1_B`·`std1_D`만 빈 것은 반쯤 채운 것이 아니다(§1.7 R1-13) — 그 두 칸에는 원문에 대응하는 진술이 없다. 검사는 한 방향으로만 읽는다. 문구가 비었으면 다섯 칸이 전부 빈다. 문구가 차 있으면 **적용 교육과정이 공시한 만큼** 찬다(5단계면 다섯 칸, 3단계면 A·C·E 세 칸).

R3-3. 성취기준을 걷어냈으면 **문서 전체에서 잔존을 검사한다.** 성취기준란만 지우고 출제 의도·유의점·차시에 남겨 두는 것이 가장 흔한 결함이다.

```bash
# 슬롯 JSON에서. 기대값 0.
python3 -c "import json,sys; print(json.dumps(json.load(open('content2_v4.json',encoding='utf-8')),ensure_ascii=False).count('9수03-13'))"

# 빌드 결과에서. HWPX 본문 텍스트를 뽑아 세는 자립 명령이다. 기대값 0.
# (전용 추출 스크립트는 tools/에 없다. 아래를 그대로 쓴다.)
python3 -c "
import zipfile,re,sys
x = zipfile.ZipFile(sys.argv[1]).read('Contents/section0.xml').decode()
t = ''.join(re.sub(r'<[^>]+>', '', m) for m in re.findall(r'<hp:t>.*?</hp:t>', x, re.S))
for w in sys.argv[2:]:
    print(w, t.count(w))" out.hwpx '9수03-13' '미확인' 'MISSING'
```

같은 명령이 R1-8의 꼬리표 검사도 겸한다. `<hp:t>`가 HWPX의 유일한 본문 텍스트 태그이므로 표 안·중첩 표 안까지 한 번에 걸린다.

v4는 이 검사로 "문서 전체 잔존 0건"을 확인했다.

R3-4. 서식이 요구하는 문항 유형 구성을 성취기준 배치보다 먼저 확인한다. 실습 서식은 "서술형 1문항 이상, 논술형 1문항 이상"을 요구하는데 네 문항을 전부 논술형으로 분류해 **제출 요건 미충족**이 된 적이 있다. 문항 1·2·4를 서술형, 문항 3을 논술형으로 정정하면서 과제 개요·차시 평가란·출제 의도를 함께 고쳤다.

## 4. 성취수준 점수 구간

### 4.0 두 층을 구분한다 — `std*_A~E`와 `level_*`

"성취수준"이라는 말이 이 스킬에서 **두 가지**를 가리킨다. 슬롯도 출처도 칸 수 규칙도 다르다. 섞으면 2015 개정 문항에서 "성취수준이 3단계인데 점수 구간은 왜 5개냐"는 잘못된 수정이 들어온다.

| | 성취기준별 성취수준 | 과제 수행수준 구간 |
|---|---|---|
| 슬롯 키 | `std1_A`~`std1_E`, `std2_A`~`std2_E` | `level_A_score`~`level_E_score`, `level_A_desc`~`level_E_desc` |
| 무엇인가 | 성취기준 하나에 학생이 어느 정도로 도달했는지를 **교육과정 문서가 미리 기술해 둔 것** | 이 과제의 **총점을 구간으로 갈라** 각 구간의 수행 특성을 기술한 것 |
| 어디서 오나 | NCIC 공시 자료에서 **글자 단위로 옮겨 온다**(§1). 계산하지 않는다 | 채점기준표의 총점과 floor에서 **§4.3 절차로 계산한다.** 옮겨 오지 않는다 |
| 칸 수 | **적용 교육과정에 따라 갈린다** — 2022 개정 A~E 5단계, 2015 개정 상·중·하 3단계(§1.6·§1.7) | **교육과정과 무관하게 언제나 A~E 5구간** |
| 문항이 바뀌면 | 안 바뀐다. 성취기준이 같으면 같다 | 총점·채점 요소가 바뀌면 다시 계산한다(R4-2·R4-7) |
| 각주 | R1-9(어느 표에서 옮겼는지)·R1-14(3단계 변환) | R4-5(총점·요소 수·floor) |
| 점검표 | §6의 1·1-1·3·7·7-1 | §6의 10·10-1·11·12·13·14·18 |

**§4 전체(4.1~4.8)의 A~E는 모두 오른쪽 열, 즉 과제 수행수준 구간을 가리킨다.** 2015 개정을 적용해 `std1_*`가 상·중·하 3단계여도 `level_*`는 A~E 5구간 그대로다. 점수 구간은 채점기준표에서 나오고, 채점기준표의 급간 구조는 평가 유형 프로파일(수행평가/지필평가)과 학교 규정에 매이지 교육과정 개정판에 매이지 않기 때문이다.

그래서 §1.6의 분기는 **§4.3의 절차·`check_levels`·`floor` 계산 어디에도 영향을 주지 않는다.** `floor`는 요소별 최저 급간의 합이고, `R`은 총점과 `floor`에서 나오며, 다섯 이름 A~E는 이 스킬이 정한 구간 이름이지 교육과정 문서에서 옮겨 온 이름이 아니다. 다만 두 가지를 지킨다.

- **`check_levels`에 `std*_A~E`를 넘기지 않는다.** 3개짜리 목록을 넘기면 첫 assert(`ABCDE`가 이 순서로 정확히 한 번씩)에서 걸리는데, 그 실패는 "3단계 자료라서" 나는 것이 아니라 **층을 잘못 넘겨서** 나는 것이다. 실패 문구를 보고 구간을 3개로 줄이지 마라.
- **`level_*_desc` 다섯 줄을 `std1_*`의 세 줄에서 베껴 만들지 않는다.** 수행 특성은 이 과제의 채점 요소에서 쓴다(§4.7). 원문 진술을 그대로 복사하면 R1-4가 요구하는 "원문과 글자 단위로 같은" 문장이 성취수준란 밖에서 다시 나타나, 어느 것이 원문인지 흐려진다.

층이 하나 더 있다 — **학기 단위 성취수준 판정**. 이것은 슬롯이 아니고 이 문항 하나로 정하지 않는다(§4.8 R4-11).

### 4.1 구간이 갖춰야 할 조건

R4-1. 구간은 **0점부터 총점까지를 겹침 없이 빠짐없이** 덮는다. 수준은 **A→E가 점수 내림차순**이다. A의 상한 = 총점, E의 하한 = 0, 이웃한 두 수준은 (아래 수준의 상한 + 1) = (위 수준의 하한). 이름만 A~E로 붙여 놓고 점수 방향을 뒤집는 것(A 0~7 … E 18~20)은 결함이다. §4.3-7의 검사는 구간을 다시 정렬하지 않고 넘긴 순서 그대로 이 방향을 본다.
R4-2. 총점이 바뀌면 구간을 **다시 나눈다.** 15점 구간을 20점 문서에 그대로 두는 것은 결함이다.
R4-3. **C가 유일하게 가장 넓다.** 다른 네 수준 중 하나라도 C와 도달 가능 점수 개수가 같으면 결함이다. 수행평가·지필평가 두 프로파일에 똑같이 적용한다. 만점은 A에 들고, 0점은 E에 든다.

### 4.2 함정 — 도달 불가 구간

구간 분할은 0단계에서 확정한 **평가 유형 프로파일**에 따라 갈린다. **수행평가 프로파일(기본값)**은 기본점수가 있어야 하므로 0점 급간을 두지 않고(최저 급간 1점), 다음이 성립한다.

```
floor = Σ(각 채점 요소의 최저 급간 점수)
      = 채점 요소가 n개이고 최저 급간이 모두 1점이면  floor = n

답안을 제출한 학생의 총점은 언제나 floor 이상이다.
0점부터 floor − 1점까지는 미응시·미제출 전용 구간이다.
```

백지에 가깝더라도 **제출한** 답안은 모든 채점 요소에서 최저 급간 1점을 받아 총점이 요소 수 n점이 되므로, 이 구간에 들어가지 않는다. 이 구간에 남는 것은 미응시·미제출뿐이다.

**개수 축(rubric-rules.md §3)을 써도 이 계산은 그대로다.** 급간을 `옳게 수행한 원자 수행의 개수`로 가르더라도 요소별 최저 급간의 **점수**는 바뀌지 않는다 — 수행평가는 `옳게 수행한 원자 0개`가 최저 1점 급간에, 지필평가는 같은 답안이 0점 급간("무응답 또는 그 외의 오답.")에 걸린다. 따라서 floor는 수행평가 n, 지필평가 0으로 종전과 같고, 아래 §4.3의 절차와 `check_levels`도 그대로 쓴다. 원자 수 m은 요소 안에서 급간을 가르는 수일 뿐 floor·총점 어디에도 들어가지 않는다.

피자 문항 v6은 채점 요소 6개(급간 4/4/4/3/3/2, 합 20점)이므로 **floor = 6**이다. 1점부터 5점까지는 어떤 답안으로도 나올 수 없다.

**지필평가(정기고사) 프로파일은 0점이 가능하므로 최저 총점이 0이다.** 요소마다 최저 급간이 0점("무응답 또는 그 외의 오답.")이어서 floor = 0이고, 도달 불가 구간 문제가 없다. 0~총점을 다섯 구간으로 자연 분할한다(그 절차의 정의는 §4.3-5에 있다. "고르게"가 C 동률을 뜻하지는 않는다). "미응시·미제출 전용 하한 구간" 처리는 수행평가 프로파일에만 필요하다. (근거 — 연수 자료의 채점기준표 예시가 이 구분과 같다: 수행평가 예시는 최저 급간이 1점이고, 정기시험 예시는 0점 급간을 둔다. 학교 학업성적관리규정이 있으면 그것이 우선한다.)

**학교 규정이 기본 프로파일과 다르면 floor·최저 급간을 규정의 실제 값으로 계산한다.** 프로파일 이름(`수행평가`/`지필평가`)에서 floor를 추정하지 말고, 규정이 정한 요소별 최저 급간을 실제로 합산한 값을 floor로 쓴다. 예를 들어 규정이 수행평가에도 요소별 0점 급간을 두게 하면 그 문서의 floor는 요소 수 n이 아니라 0이다.

R4-4. **각 수준은 도달 가능한 점수를 최소 1개 포함해야 한다.** 최저 수준(E)은 floor 이상의 값을 최소 1개 포함한다. 이것을 어기면 그 수준은 사실상 미응시 전용이 되어 아무 학생도 배정되지 않는다.

### 4.3 도달 가능 구간으로 나누는 방법

1. `floor`를 계산한다. `floor = Σ(요소별 최저 급간)`. 프로파일 이름이 아니라 실제 급간을 합산한다 — 기본 프로파일대로라면 수행평가는 요소가 n개면 floor = n, 지필평가는 최저 급간이 모두 0점이므로 floor = 0이지만, 학교 규정이 다르면 §4.2대로 규정의 실제 최저 급간을 합산한다.
2. 도달 가능 범위 `reach = [floor, 총점]`, 그 크기 `R = 총점 − floor + 1`을 구한다.
3. `R ≥ 6`인지 확인한다. 다섯 수준에 도달 가능 점수를 하나씩 두고 C를 다른 수준보다 넓게 하려면 최소 배분은 A 1 / B 1 / C 2 / D 1 / E 1이므로 **R = 6이 최소**다. 6보다 작으면 어떤 분할로도 R4-3을 만족시킬 수 없으므로 총점을 올리거나 채점 요소를 줄인다. 이 6은 순수한 산술 하한이 아니라 **"C 유일 최대"라는 이 스킬의 고정 정책(R4-3) 아래에서의 최소**다. 다섯 수준으로 나누기만 하는 하한 `R ≥ 5`와 혼동하지 마라.
4. `R`을 다섯 점수 구간 A~E에 배분한다. **R이 6~9이면 축소형 A 1 / B 1 / C 나머지(가장 넓게) / D 1 / E 1**을, **R이 10 이상이면 권장형 A 2~3 / B 2 / C 나머지(가장 넓게) / D 2 / E 1~2**를 쓴다. 축소형은 실제로 성립한다 — 수행평가 10점·채점 요소 4개면 floor = 4, R = 7이고 `A 10 / B 9 / C 6~8 / D 5 / E 0~4`는 도달 가능 개수가 1/1/3/1/1로 다섯 수준 모두 도달 가능하며 C가 가장 넓다.
5. 4번의 폭은 **수행평가 프로파일의 권장 폭**이다. 수행평가는 도달 가능 범위 floor~총점에 위 폭을 적용한다. **지필평가는 floor = 0이므로 0~총점의 `R = 총점 + 1`개 점수를 아래 세 줄로 나눈다.** 이 절차가 "자연 분할"의 정의다.

   ⑴ C의 크기를 `C = ⌈(R + 4) / 5⌉`로 먼저 정한다. 나머지 넷을 아무리 고르게 나눠도 C가 그 넷보다 1 이상 넓어지는 **최솟값**이다(다른 넷이 각각 `C − 1` 이하여야 하므로 `R ≤ 5C − 4`).
   ⑵ 남은 `R − C`개를 A·B·D·E에 몫만큼 똑같이 주고, 남는 나머지를 `A → B → D → E` 순으로 하나씩 더 준다.
   ⑶ 점수 내림차순으로 A부터 채워 구간을 만든다. 검산: C가 **유일한 최대**이고, 다섯 개수의 최대 − 최소 ≤ 2다.

   20점 지필이면 `R = 21`, `C = ⌈25 / 5⌉ = 5`, 남은 16을 4씩 나누어 **`A 17~20 / B 13~16 / C 8~12 / D 4~7 / E 0~3`**(개수 4/4/5/4/4)이 된다.

   **"고르게"를 먼저 적용하고 나머지를 C부터 주는 방식은 쓰지 않는다.** `R mod 5`가 0이면 다섯이 모두 같아지고, 2~4면 C가 A와 동률이 되어 R4-3과 7번 검사에 걸린다. 이 스킬은 지필에서도 C 동률을 허용하지 않고 **C 유일 최대**로 확정한다 — 그래서 C 크기를 먼저 못 박고 나머지를 고르게 나눈다. 반대로 20점 지필에 4번의 수행형 권장 폭(A·B·D·E에 2~3칸씩만 주고 나머지를 전부 C에 몰아주는 방식 — C가 12칸 안팎이 된다)을 그대로 적용해 C를 과도하게 넓히지도 마라. ⑴~⑶은 C를 "나머지 전부"가 아니라 **`⌈(R + 4) / 5⌉`라는 정해진 크기**로 못 박는다.
6. E의 하한을 0으로 늘려 미응시 구간을 흡수시킨다. 나머지 구간은 그대로 둔다. (지필평가 프로파일은 floor = 0이라 흡수할 구간이 없으므로 이 단계를 건너뛴다.)
7. 아래 검사를 돌린다.

```python
PROFILE = "수행평가"   # 0단계에서 확정한 평가 유형: "수행평가" | "지필평가"
assert PROFILE in ("수행평가", "지필평가"), f"평가 유형 오타: {PROFILE!r}"


def check_levels(levels, total, floor):
    # levels: [("A",lo,hi), ("B",...), ("C",...), ("D",...), ("E",lo,hi)] 점수 내림차순
    # floor: 답안을 제출한 학생의 최저 총점 = Σ(요소별 최저 급간).
    #        프로파일 이름에서 추정하지 말고 실제 급간 합을 넘긴다(§4.2·§4.3-1).
    assert [name for name, _, _ in levels] == list("ABCDE"), \
        "수준은 A,B,C,D,E가 이 순서로 정확히 한 번씩 있어야 함"

    assert 0 <= floor <= total, \
        f"floor 범위 오류: {floor} (total={total})"

    R = total - floor + 1
    assert R >= 6, \
        f"도달 가능 점수 {R}개로 C가 가장 넓은 5수준을 만들 수 없음"

    for name, lo, hi in levels:
        assert 0 <= lo <= hi <= total, \
            f"{name} 구간 범위/방향 오류: {lo}~{hi}"

    assert levels[0][2] == total, \
        "A 수준 상한이 총점이 아님"
    assert levels[-1][1] == 0, \
        "E 수준 하한이 0이 아님"

    # levels는 A→E, 즉 높은 점수 구간부터 낮은 구간 순서다.
    # 구간을 lo로 다시 정렬하지 않는다. 정렬하면 이름만 ABCDE로 두고
    # 점수 구간만 뒤집은 역전(A 0~7 … E 18~20)이 통과해 버린다.
    for higher, lower in zip(levels, levels[1:]):
        assert lower[2] + 1 == higher[1], \
            f"구간 불연속/중복/순서 역전: {higher} {lower}"

    counts = {}
    for name, lo, hi in levels:
        counts[name] = sum(s >= floor for s in range(lo, hi + 1))
        assert counts[name] > 0, \
            f"{name} 수준에 도달 가능 점수가 없음 ({lo}~{hi}, floor={floor})"

    # 두 프로파일 모두 C 유일 최대다(§4.3-5). 동률도 여기서 걸린다.
    assert counts["C"] > max(counts[x] for x in "ABDE"), \
        f"C가 유일한 최대가 아님: {counts}"

    return True
```

호출할 때는 프로파일 이름에서 floor를 다시 추정하지 말고, 채점기준표의 **실제 요소별 최저 급간을 합산한 값**을 넘긴다.

```python
# element_grades: 요소별 급간 점수 목록. 예) 수행 6요소 = [[4,3,2,1],[4,3,2,1],...]
#                                        지필 6요소 = [[4,3,2,1,0],[4,3,2,1,0],...]
floor = sum(min(scores) for scores in element_grades)   # 수행 6요소 → 6, 지필 6요소 → 0
total = sum(max(scores) for scores in element_grades)
check_levels(levels, total, floor)
```

이 구간은 자동 산출된 **과제 수행수준 구간**이며(§4.0), 성취기준별 성취수준·학기 성취수준 판정과 다르다. 경계(예: 18점=A, 17점=B)는 교사가 경계 답안을 검토해 조정할 수 있다. 자동 검사가 보는 것은 수준 이름, 점수 방향(A 상한 = 총점, E 하한 = 0, A→E 내림차순), 구간의 겹침·공백, 수준별 도달 가능성, C 유일 최대까지다. 지필 자연 분할의 "고르게"(5번 ⑶의 최대 − 최소 ≤ 2)는 코드가 아니라 5번의 검산으로 사람이 확인한다 — `check_levels`는 프로파일을 인자로 받지 않아 `floor == 0`만으로는 지필평가와 학교 규정이 0점 급간을 넣은 수행평가를 구분할 수 없기 때문이다. 경계를 조정했으면 조정한 값으로 5번 검산과 7번 검사를 **다시** 돌린다.

### 4.4 나쁜 예와 좋은 예

두 문서 모두 총점 20점, 채점 요소 6개, floor = 6이다.

`references/hwpx-build.md`의 R7은 올바른 값(`18~20 / 16~17 / 10~15 / 8~9 / 0~7`)과 아래 나쁜 예를 **결함 확인 사례로 구분해** 싣고 있고, few-shot 예시 `examples/valid-similarity-20pt.json`도 도달 가능형 구간을 쓴다. 예시 파일은 조판 좌표와 문체를 보여 주는 용도이지 점수 구간의 본보기가 아니다. **구간은 반드시 §4.3의 절차로 새로 계산하고, 예시 파일에서 복사하지 않는다.**

- 나쁜 예 — 15점 기준 비율을 20점에 그대로 늘렸다. (`content2_v6.json`에 남아 있는 값이며, `references/hwpx-build.md` R7이 결함 사례로 인용한다.)

| 수준 | 구간 | 도달 가능 점수 |
|---|---|---|
| A | 19~20점 | 19, 20 |
| B | 16~18점 | 16, 17, 18 |
| C | 8~15점 | 8 ~ 15 |
| D | 4~7점 | **6, 7** (4·5는 나올 수 없음) |
| E | 0~3점 | **없음** — 0점(미응시)뿐 |

E에는 답안을 낸 학생이 절대 배정되지 않는다.

- 좋은 예 — 도달 가능 구간 6~20점(R = 15)을 다섯으로 나눈 뒤 E의 하한만 0으로 늘렸다. (`content_b_v6.json`)

| 수준 | 구간 | 도달 가능 점수 | 개수 |
|---|---|---|---|
| A | 18~20점 | 18, 19, 20 | 3 |
| B | 16~17점 | 16, 17 | 2 |
| C | 10~15점 | 10 ~ 15 | 6 |
| D | 8~9점 | 8, 9 | 2 |
| E | 0~7점 | 6, 7 | 2 |

3 + 2 + 6 + 2 + 2 = 15 = R. 0~5점은 E 안에 흡수되어 미응시·미제출을 받는다.

- 15점판(v5)도 같은 결함이 있다. 채점 요소 6개, floor = 6인데 구간이 A 14~15 / B 12~13 / C 6~11 / D 3~5 / E 0~2였다. **D와 E 모두 도달 가능 점수가 0개다.** 규칙대로 다시 나누면 R = 10이고 다음이 된다.

```
A 14~15  B 12~13  C 9~11  D 7~8  E 0~6
(도달 가능 개수 2 + 2 + 3 + 2 + 1 = 10 = R)
```

### 4.5 근거 표기

R4-5. 성취수준 구간 표 **바로 아래에 한 줄 각주**로 산출 근거를 남긴다. 총점·요소 수·floor를 모두 적는다.

> ※ 채점 요소 6개의 최저 급간이 각각 1점이므로 답안을 제출한 학생의 최저 총점은 6점이다. 구간은 6점부터 20점까지를 다섯 수준으로 나누었고, 0점부터 5점까지는 미응시·미제출에 해당한다.

지필평가 프로파일의 각주 예 — "※ 각 채점 요소에 0점 급간(무응답 또는 그 외의 오답)이 있으므로 최저 총점은 0점이다. 구간은 0점부터 20점까지를 다섯 수준으로 나누었다."

R4-6. 작업 노트에도 기계 검사가 가능한 형태로 남긴다. `profile` 키에 평가 유형을 적는다.

```json
"interval_basis": {"profile": "수행평가", "total": 20, "n_elements": 6, "min_grade": 1, "floor": 6, "reachable": "6..20", "R": 15}
```

지필평가 프로파일이면 `{"profile": "지필평가", "total": 20, "n_elements": 6, "min_grade": 0, "floor": 0, "reachable": "0..20", "R": 21}`처럼 `min_grade` 0·`floor` 0으로 남긴다.

### 4.6 총점을 바꾸면 같이 고치는 칸

R4-7. 총점 변경은 성취수준 구간만 바꾸는 일이 아니다. 다음을 **한 번에** 고치고 전수 대조한다.

- 발문의 소문항 배점 표기 — `(4점)(4점)(7점)(5점)`
- 도입부 총점 — `※ … 물음에 답하시오. [총 20점]`
- 과제 개요의 배점
- 차시 계획의 평가란에 적힌 배점
- 채점기준표 합계 행
- 과제 수행수준 구간 `level_*_score`의 A~E (§4.0의 오른쪽 층)
- 성취수준별 수행 특성(`level_*_desc`)에 배점이 인용되어 있으면 그것도

검사: 요소별 최고 급간의 합 = 문항별 배점의 합 = 합계 행 = 도입부 총점 = A수준 상한.

**`std1_*`·`std2_*`는 이 목록에 없다.** 성취기준별 성취수준은 총점과 무관하므로 총점을 바꿔도 건드리지 않는다(§4.0). 총점을 고치면서 성취기준별 성취수준 문면을 함께 손보는 것은 R1-4 위반이다.

### 4.7 성취수준별 수행 특성 진술

R4-8. 각 수준은 **3문장 이내**로 쓴다. 학생에게 그대로 돌려주는 피드백 문체(`~할 수 있습니다`, `~기 바랍니다`)를 쓴다.
R4-9. 구조는 A·B는 `도달한 것 → 도달한 것 → 한계`, C·D·E는 `도달한 것 → 한계 → 처방`이다. **하위 세 수준(C·D·E)에는 처방 문장을 반드시 1개 넣는다.**
R4-10. 위계 조절 어휘(`다양한 방법으로`, `간단한`, `안내된 절차에 따라`, `도움을 받아`)는 **성취수준 진술에만** 쓴다. 채점기준표 급간 진술에는 쓰지 않는다. 급간을 가르는 축은 `옳게 수행한 원자 수행의 개수` 하나다(rubric-rules.md §3).

여기서 쓰기 규칙으로 작동하는 "성취수준 진술"은 **`level_*_desc`**다. `std*_A~E`에도 같은 어휘가 나타나지만 그것은 원문을 글자 단위로 옮긴 결과이므로 고를 여지가 없다(R1-4). 원문에 그 어휘가 없다고 해서 넣지도, 있다고 해서 빼지도 않는다.

- 좋은 예 (E수준, 세 문장이 도달·한계·처방으로 나뉜다)
  > 안내된 절차에 따라 지름에서 반지름을 구하고 원의 넓이 공식에 넣어 한 피자의 윗면 넓이를 구하는 것과 같은 활동을 도움을 받아 수행할 수 있습니다.
  > 그러나 지름의 비를 가장 간단한 자연수의 비로 나타내거나, 길이·넓이·부피의 비를 구분하는 데 어려움을 겪습니다.
  > 닮음의 뜻과 닮음비 구하기, 원의 넓이 공식, (기둥의 부피) = (밑넓이) × (높이)를 먼저 복습한 뒤, 모눈종이의 원 칸 세기(반지름 4칸과 8칸)와 쌓기나무 8개 쌓기로 4배·8배를 직접 확인하기 바랍니다.

### 4.8 한 문항으로 성취수준을 확정하지 않는다

R4-11. 문항이 성취기준의 일부만 평가 요소로 설정했다면 그 문항만으로 모든 성취수준을 판단할 수 없다. 이 한계를 〈채점 시 유의점〉에 명시한다.

> 본 문항은 [9수03-12]의 일부를 평가 요소로 설정한 것이므로, 이 문항만으로 모든 성취수준을 정확하게 판단하기는 어렵습니다. 문항 2·3의 결과와 형성평가 기록을 함께 고려하여 학기 단위 성취수준을 판정합니다.

R4-12. 차시 계획의 마지막 평가란에도 같은 취지를 적는다: `문항 1부터 문항 4까지의 결과와 형성평가 기록을 종합하여 성취수준 판정(수행평가 단독으로 확정하지 않음)`.

## 5. 교과 역량 표기

### 5.1 쓸 수 있는 명칭

R5-1. 경기 2025 자료집 20개 문항 정보표를 전수 집계한 결과 쓰인 명칭은 **다섯 개**뿐이다.

| 명칭 | 20문항 중 등장 |
|---|---|
| 문제해결 역량 | 18 |
| 의사소통 역량 | 16 |
| 추론 역량 | 11 |
| 연결 역량 | 7 |
| 정보처리 역량 | 3 |
| 창의·융합 역량 | **0** |
| 태도 및 실천 역량 | **0** |

R5-2. `창의·융합`, `태도 및 실천`은 2015 개정 계열 명칭이고 이 자료집에 0회 등장한다. 2022 개정 문서를 근거로 쓰려면 NCIC 원문에서 명칭을 확인한 뒤 쓰고, **확인하지 못하면 쓰지 않는다**(R1-5 적용).
R5-3. 표기는 `추론 역량`처럼 **`역량`을 붙인 완전한 명칭**으로 쓴다. `추론`만 적지 않는다.

### 5.2 개수와 배치

R5-4. 한 문항 정보표에 **2~3개**를 쓴다. 경기 20문항 실측 분포는 3개 13건, 2개 3건, 4개 2건, 1개 2건으로 3개가 최빈이다. 5개를 전부 나열하지 않는다.
R5-5. 문항 정보표의 열 순서는 **문항 | 교과 역량 | 성취기준 | 평가 요소**다. 소문항이 여러 개면 교과 역량 칸은 세로 병합한다.

```
문항      교과 역량            성취기준                        평가 요소
1-1
       추론 역량
1-2   문제해결 역량   [9수03-05] 다각형의 성질을 이해하고   ∙ 정다각형의 내각의 크기의 합
      의사소통 역량   설명할 수 있다.                       구하기
1-3
```

R5-6. **서식에 교과 역량 칸이 없으면 만들지 않는다.** 경기 양식에는 있고 이번 실습 제출 서식(`content2_*.json`)에는 없다. 슬롯 키에 없는 항목을 억지로 끼워 넣지 않는다.

### 5.3 고르는 방법

R5-7. 평가 요소의 **수행 동사**에서 역량을 끌어온다. 발문의 반응지시어도 같은 동사에서 나오므로 셋이 자동으로 정렬된다.

| 평가 요소의 동사 | 붙이는 역량 |
|---|---|
| 관계를 설명하기 / 왜 그런지 정당화하기 / 일반화하기 | 추론 역량 |
| 실생활 문제 해결하기 / 판단하기 / 방안 고르기 | 문제해결 역량 |
| 근거를 들어 서술하기 / 반박하기 / 설득하는 글로 조직하기 | 의사소통 역량 |
| 표·식·그래프를 오가며 나타내기 / 다른 단원·교과와 잇기 | 연결 역량 |
| 자료를 정리·해석하기 / 계산기·공학 도구 쓰기 | 정보처리 역량 |

R5-8. 채점 요소로 점수화하지 않는 역량은 붙이지 않는다. 역량 칸에 적었으면 그 역량을 요구하는 채점 요소가 최소 하나 있어야 한다(§2 사슬과 동일).

- 나쁜 예 — 채점 요소가 전부 계산과 설명인데 역량 칸에 다섯 개를 늘어놓았다.
  > 추론 역량 / 문제해결 역량 / 의사소통 역량 / 연결 역량 / 정보처리 역량 / 창의·융합 역량 / 태도 및 실천 역량

- 좋은 예 — 세 개, 채점 요소 세 종류(합 구하기 / 가능성 설명하기 / 정다각형 구하기)에 각각 대응한다.
  > 추론 역량 / 문제해결 역량 / 의사소통 역량

## 6. 최종 점검표

문서를 넘기기 전에 전부 통과해야 한다.

| # | 검사 | 기대값 |
|---|---|---|
| 1 | 성취기준 문구·성취수준이 **적용 교육과정**(§1.6)의 NCIC 자료 원문과 글자 단위로 일치 | 불일치 0 |
| 1-1 | 실시 연도 × 학년으로 적용 교육과정·게시글·수준 체계를 판정해 작업 노트 `curriculum`과 `purpose` 각주에 기록 (R1-12·R1-14) | 각 1건 존재 |
| 2 | 빌드 텍스트에 `미확인` / `MISSING` 잔존 | 0건 |
| 3 | 성취수준 출처(성취기준별/영역별) 각주 | 1줄 존재 |
| 4 | 고아 채점 요소 / 고아 평가 요소 | 각 0건 |
| 5 | 성취기준란의 모든 코드가 차시·문항 정보·채점 요소 세 곳에 등장 | 누락 0 |
| 6 | 걷어낸 성취기준 코드의 문서 전체 잔존 | 0건 |
| 7 | `std2_text`가 비었으면 `std2_A`~`std2_E`도 전부 빈 문자열 | 참 |
| 7-1 | 3단계(2015)로 판정했으면 `std1_A`·`std1_C`·`std1_E`만 차고 `std1_B`·`std1_D`는 빈 문자열, 값 복제 0건. 5단계(2022)로 판정했으면 다섯 칸이 다 참 (R1-13) | 참 |
| 8 | 성취기준 밖 요소(문장력·글씨·창의성·단위 표기·태도)가 채점 요소에 있는지 | 0건 |
| 9 | 역방향 대조표 — 근거 없는 채점 요건 / 채점되지 않는 조건 | 각 0건 |
| 10 | `check_levels(levels, total, floor)` 통과 — §4.3의 `PROFILE`을 평가 유형으로 맞추고, `levels`는 **A→E 점수 내림차순** 그대로, `floor`는 요소별 최저 급간의 실제 합으로 계산해 넘긴 뒤 실행 | 통과 |
| 10-1 | 지필평가면 §4.3-5의 자연 분할 검산 — C 유일 최대, 다섯 개수의 최대 − 최소 ≤ 2 | 참 |
| 11 | 요소별 최고 급간 합 = 문항 배점 합 = 합계 행 = 도입부 총점 = A수준 상한 | 전부 일치 |
| 12 | `level_*_score` 구간 산출 근거 각주 (R4-5) | 1줄 존재 |
| 13 | `level_*_desc`가 수준당 3문장 이내, C·D·E에 처방 문장 1개 | 참 |
| 14 | 위계 조절 어휘가 채점기준표 급간 진술에 등장 | 0건 |
| 15 | 교과 역량 칸이 있는 서식이면 명칭이 5종 안에 있고 개수가 2~3개 | 참 |
| 16 | `item{n}_form`에 서술형 1개 이상 + 논술형 1개 이상 (R3-4) | 참 |
| 17 | 〈채점 시 유의점〉에 한 문항으로 성취수준을 확정하지 않는다는 문장, 마지막 차시 평가란에 같은 취지 (R4-11·R4-12) | 각 1문장 존재 |
| 18 | `level_*_score`가 과거 결함 구간(`19~20 / 16~18 / 8~15 / 4~7 / 0~3`)과 같은 값인지 | 같으면 §4.3으로 재계산 |
